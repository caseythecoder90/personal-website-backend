spring:
  application:
    name: personal-website-test

  datasource:
    url: jdbc:postgresql://localhost:5432/casquinn_personal_website_test
    username: casquinn
    password: Ku^a4MoADdqLf4n>ka,d
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        globally_quoted_identifiers: true
        globally_quoted_identifiers_skip_column_definitions: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true

  data:
    redis:
      host: localhost
      port: 6379
      password: vHZBDJUzyG}1AE6dBMZY

logging:
  level:
    com.caseyquinn.personal_website: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
